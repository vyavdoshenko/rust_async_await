# This repository created to try examples code from async/await rust lang book.

https://rust-lang.github.io/async-book/

* Build release cargo project:
```
$ cargo build --release
```
* Build and run a release cargo project:
```
$ cargo run --release
```
